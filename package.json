{
  "name": "zotseek",
  "version": "1.13.1",
  "description": "ZotSeek - Semantic Search for Zotero using local AI embeddings",
  "author": "José Fernandes",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.js",
    "build:dev": "node scripts/build.js --dev",
    "watch": "node scripts/build.js --watch",
    "start": "npm run build && npm run start:zotero",
    "start:zotero": "open -a Zotero --args -purgecaches -ZoteroDebugText -jsconsole",
    "release": "node scripts/release.js"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "archiver": "^7.0.1",
    "bumpp": "^10.4.0",
    "esbuild": "^0.19.8",
    "typescript": "^5.3.2"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.8.1",
    "zotero-plugin-toolkit": "^5.1.0-beta.13"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
